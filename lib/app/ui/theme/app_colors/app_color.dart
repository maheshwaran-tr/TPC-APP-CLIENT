import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const Color primaryColor = Color(0xFFEEF8FF);
  static const Color secondaryColor = Color(0xFF3F70B6);
  static const Color tertiaryColor = Color(0xFFFFFFFF);
  static const Color textColorLight =
      Colors.black; // Black text for light theme

  // Dark Theme Colors
  static const Color primaryColorDark = Color(0xFF000000);
  static const Color secondaryColorDark = Color(0xFF5A5A5A);
  static const Color tertiaryColorDark = Color(0xFFFFFFFF);
  static const Color textColorDark = Colors.white; // White text for dark theme

  // Background Colors
  static const Color backgroundColor =
      Color(0xFFF6F6F6); // Light theme background
  static const Color backgroundColorDark =
      Color(0xFF121212); // Dark theme background
}
